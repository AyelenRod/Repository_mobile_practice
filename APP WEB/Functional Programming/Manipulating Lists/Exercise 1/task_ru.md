## Работа со списками (#1)

Реализуйте функцию `zipWithIndex()` с использованием `zip()`. Верните `List` из `Pair`, где первый элемент в `Pair` — это элемент, а второй элемент — это индекс этого элемента.