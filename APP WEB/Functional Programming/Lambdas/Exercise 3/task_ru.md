## Лямбды (#3)

Стартовый код содержит `data` класс для `Student`. Реализуйте функцию `registerStudents(names: List<String>, startId: Int = 0)`, чтобы создать `List<Student>` из `List<String>`. Назначайте разным студентам `id`, начиная с `startId`.