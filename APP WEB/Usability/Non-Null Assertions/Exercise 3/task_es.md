## Afirmaciones no nulas (#3)

Implementa `absMinIsMax(ints: List<Int>)` que verifica si el valor absoluto del mínimo en `ints` es igual al valor absoluto del máximo en `ints`. Si la lista está vacía, devuelve `false`.