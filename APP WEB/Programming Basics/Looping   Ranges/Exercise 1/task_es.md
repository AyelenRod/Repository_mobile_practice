## bucles & rangos (#1)

Usando un bucle `for`, crea una función que calcule el factorial de su parámetro (`n! = 1 * 2 * ... * n`).

Coloca una `L` al final de un literal numérico o usa `.toLong()` para convertir una constante entera o expresión al tipo `Long`.