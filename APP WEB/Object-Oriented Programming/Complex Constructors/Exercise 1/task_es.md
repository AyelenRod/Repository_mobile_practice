## Constructores complejos (#1)

Crea una clase `Alien` con `name: String` como un parámetro del constructor. Añade una propiedad `val` llamada `myName` y asigna `name` a la propiedad `myName` dentro de la sección `init`.