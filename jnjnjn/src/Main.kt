import java.time.LocalDate
import java.time.LocalTime
import javax.xml.namespace.QName

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.

fun main() {
   /* var name = "Kotlin"
    var name2: String = ""
    for (c : Char in name) {
        if (c == 'A'){
            name2 = c.toString()
        }
    }

    name = name2
    println(name)


    var itemList: Array<Int> = arrayOf(1,2,3,4,5)
    // inicializar solo - var itemList: Array<Int>

    itemList[6] = 10
    println(itemList)

    var userList: List<String> = listOf("Juan", "Kotlin", "Java") //inmutable
    var userList2: MutableList<String> = mutableListOf("Juan", "Kotlin", "Java") //mutable
    userList2.add("Juan")


    var number: Int? = readln().toIntOrNull()
    print(number)

       var listofPerson: List<String> = listOf("Alix", "Aurora", "Ayelen", "Regina", "Mishell", "luvia")
    listofPerson = listofPerson.filter{
        it.contains("i")
    }
    val sb = StringBuilder("An existing string and list: ")
    println(listofPerson.joinTo(buffer = sb, separator = ",", prefix = "" ))

    var Student: Person = Student("Ayelen")
    //Student.sayHello()
    var Studentname = Student.createUnitStudent()
*/




}